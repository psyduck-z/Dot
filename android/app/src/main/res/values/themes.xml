<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Plain platform theme rather than AppCompat: the whole UI is a WebView,
         so pulling in a UI toolkit would add weight for nothing. Black
         throughout so there is no white flash before the page paints. -->
    <style name="DotTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
